pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,vue}"
      run: npm run check:staged -- {staged_files}
      stage_fixed: true
    line-limit:
      glob: "*.{js,vue,mjs}"
      run: bash scripts/check-line-limit.sh {staged_files}
